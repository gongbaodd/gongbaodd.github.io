language: node_js
before_script:
  - npm install -g gatsby-cli
script:
  - yarn build
  - yarn es:check
  - yarn cy:test
  - yarn cp:cname
  - yarn deploy
  - yarn deploy:coding
cache:
  npm: true
  directories:
    - ~/.cache
    - ./cypress
